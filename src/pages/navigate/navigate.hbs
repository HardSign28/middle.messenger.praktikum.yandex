<div class="page-navigate">
	<div class="container">
		<div class="card">
			<h1>Навигация</h1>
			<ul>
				{{#each pages}}
					{{#unless (eq @key "nav")}}
						<li><a href="#" page="{{@key}}">{{@key}}</a></li>
					{{/unless}}
				{{/each}}
			</ul>
		</div>
	</div>
</div>

